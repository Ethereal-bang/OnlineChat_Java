<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="online_chat_server.mapper.ContactMapper">

    <insert id="add">
        insert into contact (own, contact, state)
            values (#{own}, #{contact}, 0), (#{contact}, #{own}, 4);
    </insert>

    <select id="search" resultType="ContactItem">
        select c.id, u.id as uid, name, avatar, word, score, state
        from (select id, name, avatar, word from user where name like concat('%', #{keyword}, '%')) u,
             (select id, own, contact, score, state from contact) c
        where (c.own = #{id} and c.id = u.id);   /*因为要在本人的好友里search*/
    </select>

    <select id="getAddList" parameterType="int" resultType="ContactItem">
        select contact.id as id, own as uid, score, state, name, avatar, word, password
        from contact, user
        where own = #{id}
          and state = 4
          and user.id = contact.contact;
    </select>

</mapper>